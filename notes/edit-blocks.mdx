
# Editable Blocks

- Enable editable
  - custom pragma
  - built-in to component
  - disabled on production
- trigger edit state
  - onClick
  - via parent UI selection
- edit
  - block type
  - props
  - styles
- UI
  - toolbar
  - inline/above - pushes UI down but doesn't move
  - controls
    - color picker
    - spacing
    - preconfigured components?
